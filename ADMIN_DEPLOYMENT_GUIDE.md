# 📋 Инструкция для Админа: Развертывание на Coolify

**Для:** Administrator
**Дата:** 20 декабря 2025
**Проект:** CRM Customs - SiteMain
**GitHub:** https://github.com/crmcustoms/SiteMain

---

## 🚀 Быстрый старт (5 шагов)

### Шаг 1: Подключить GitHub к Coolify

1. Откройте **Coolify Dashboard**
2. Нажмите **"New Project"**
3. Выберите **GitHub** как источник
4. Выберите репо: `crmcustoms/SiteMain`
5. Выберите branch: `main`

---

### Шаг 2: Настроить Environment Variables в Coolify

В **Coolify → Project Settings → Environment Variables** добавьте:

```
NOTION_SECRET=<your_notion_secret_from_developer_portal>
NOTION_DATABASE_ID=<your_notion_database_id>
```

⚠️ **ВАЖНО:** Сохраните эти значения в безопасном месте! Это чувствительные данные.
⚠️ **ПРИМЕЧАНИЕ:** Владелец проекта предоставит реальные значения секретов!

---

### Шаг 3: Настроить Docker конфигурацию

Coolify должен использовать:
- **Dockerfile:** `./Dockerfile` (есть в репо)
- **Port:** `3000` (по умолчанию)
- **Health Check:** `/api/health` (для мониторинга)

---

### Шаг 4: Настроить Домен (опционально)

1. В Coolify → Project Settings → Domains
2. Добавьте ваш домен (например: `crmcustoms.com`)
3. Coolify автоматически создаст SSL сертификат

---

### Шаг 5: Развернуть

1. Нажмите **"Deploy"** в Coolify
2. Дождитесь завершения (2-5 минут)
3. Проверьте URL вашего приложения

✅ **Готово!** Сайт работает!

---

## 📊 Что произойдет автоматически:

```
GitHub push (вы делаете)
        ↓
Coolify получает webhook (автоматически)
        ↓
Coolify собирает Docker образ (автоматически)
        ↓
Coolify развертывает контейнер (автоматически)
        ↓
Сайт обновляется на сервере (автоматически)
```

**Дальше:** При каждом `git push` в `main` - автоматический деплой! 🚀

---

## 🔧 Файлы которые Coolify использует

| Файл | Назначение |
|------|-----------|
| `Dockerfile` | Конфигурация Docker образа |
| `docker-compose.yml` | Конфигурация сервисов |
| `.env.example` | Шаблон переменных (для справки) |
| `next.config.mjs` | Конфигурация Next.js |
| `package.json` | Зависимости и скрипты |

---

## 📡 Мониторинг и логи

### Просмотр логов в Coolify:
1. Project → Monitoring → Logs
2. Смотрите ошибки в реальном времени

### Health Check:
- Coolify автоматически проверяет: `https://ваш-домен.com/api/health`
- Если всё ОК - будет зеленый статус ✅

---

## ⚙️ Переменные окружения (подробно)

### Требуемые:
```
NOTION_SECRET=<your_notion_secret_from_developer_portal>
```
- Получить: https://www.notion.so/my-integrations
- Это: Internal Integration Token из вашей Notion интеграции
- **Владелец проекта предоставит это значение!**

```
NOTION_DATABASE_ID=<your_notion_database_id>
```
- Получить: Открыть базу в Notion → ID в URL
- Это: ID базы где хранятся блог посты и кейсы
- **Владелец проекта предоставит это значение!**

### Опциональные:
```
DISABLE_BLOG_CACHE=false          # Включить кэш (по умолчанию включен)
NEXT_TELEMETRY_DISABLED=1         # Отключить телеметрию Next.js
```

---

## 🛠️ Troubleshooting

### Сайт не грузится
- Проверьте переменные окружения в Coolify
- Посмотрите логи в Coolify Dashboard

### Образ не собирается
- Проверьте лог Docker build в Coolify
- Убедитесь что Dockerfile есть в репо

### Health check падает
- Проверьте что порт 3000 открыт
- Убедитесь что API работает: `/api/health`

---

## 📞 Контакты

При проблемах свяжитесь с разработчиками.
