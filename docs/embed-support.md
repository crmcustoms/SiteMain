# Поддержка Embed-контента в блоге

## Обзор

В проекте добавлена полная поддержка embed-контента из различных платформ. Это позволяет вставлять аудио, видео и интерактивный контент прямо в статьи блога.

## Поддерживаемые платформы

### 🎵 **Аудио платформы**
- **SoundCloud** - аудиотреки и плейлисты
- **Spotify** - треки, альбомы, плейлисты

### 🎥 **Видео платформы**  
- **YouTube** - видео и плейлисты
- **Vimeo** - видео контент

### 🔧 **Платформы разработки**
- **CodePen** - интерактивные примеры кода
- **Figma** - дизайн и прототипы

### 📱 **Социальные сети**
- **Twitter/X** - твиты и треды

### 🌐 **Универсальная поддержка**
- Любые другие iframe-совместимые платформы

## Как использовать в Notion

### Шаг 1: Добавление embed-блока
1. В Notion наберите `/embed` 
2. Выберите "Embed" из списка
3. Вставьте URL контента

### Шаг 2: Типы URL'ов

#### SoundCloud
```
https://w.soundcloud.com/player/?url=https%3A//api.soundcloud.com/tracks/TRACK_ID
```

#### Spotify  
```
https://open.spotify.com/embed/track/TRACK_ID
https://open.spotify.com/embed/playlist/PLAYLIST_ID
```

#### YouTube
```
https://www.youtube.com/embed/VIDEO_ID
https://youtu.be/VIDEO_ID
```

#### CodePen
```
https://codepen.io/user/embed/pen_id
```

## Техническая реализация

### Серверная обработка
Блоки `embed` обрабатываются в функции `renderNotionContent()` в файле `lib/blog.ts`:

```typescript
case 'embed':
  const embedUrl = block.embed?.url || '';
  // Автоматическое определение платформы по URL
  // Генерация соответствующего iframe кода
```

### Клиентская оптимизация
Компонент `DirectHtmlContent` добавляет:
- Ленивую загрузку iframe'ов (`loading="lazy"`)
- Обработку ошибок загрузки
- Логирование для отладки

### Стилизация
Все embed'ы получают:
- Адаптивную ширину (100%)
- Скругленные углы (`rounded-lg`)
- Подходящие пропорции для каждого типа контента
- Подписи под контентом (если добавлены)

## Безопасность

- Все iframe'ы имеют соответствующие атрибуты `allow`
- URL'ы проверяются на соответствие известным платформам
- Добавлены обработчики ошибок для некорректных ссылок

## Отладка и тестирование

### Тестовая страница
```
http://localhost:3000/test-embeds.html
```

### Логи в консоли
- `"Найдено X iframe'ов в HTML-контенте"`
- `"iframe X: [URL]... загружен успешно"`  
- `"Ошибка загрузки iframe"`

### Скрипт тестирования
```powershell
.\test-embeds.ps1
```

## Устранение проблем

### SoundCloud не отображается
1. **Проверьте тип блока** - должен быть `embed`, не `video`
2. **Проверьте URL** - используйте embed URL из SoundCloud
3. **Проверьте консоль** - ищите ошибки CORS или блокировки

### Получение правильного embed URL

#### SoundCloud:
1. Откройте трек на SoundCloud
2. Нажмите "Share" 
3. Выберите "Embed"
4. Скопируйте URL из `src` атрибута iframe

#### Spotify:
1. Откройте трек/плейлист в Spotify Web
2. Нажмите "..." → "Share" → "Embed track"
3. Скопируйте URL из `src` атрибута

### Общие проблемы
- **Блокировка HTTPS/HTTP** - убедитесь, что embed URL использует HTTPS
- **Политики CORS** - некоторые платформы могут блокировать embedding
- **Неправильный URL** - используйте специальные embed URL'ы, не обычные ссылки

## Планы развития

- [ ] Поддержка Instagram embed'ов
- [ ] Поддержка TikTok embed'ов  
- [ ] Автоматическое определение размеров
- [ ] Предварительные превью для embed'ов
- [ ] Кеширование embed-контента